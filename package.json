{
  "name": "actions-aws-deploy-to-s3",
  "version": "1.0.0",
  "description": "Copy your local dir to S3 bucket",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "ncc build index.js -o dist"
  },
  "author": "Darko Antonijevic",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.2.0",
    "aws-sdk": "^2.597.0",
    "glob": "^7.1.6",
    "mime-types": "^2.1.26"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.21.0"
  }
}
